import type { MantineThemeComponent } from '@mantine/core';

const Text: MantineThemeComponent = {
  defaultProps: {
    size: 'md',
    variant: 'text',
    span: false,
    inherit: false,
    color: 'dimmed',
  },
};

export default Text;
